TASK 9 — Stale Dual-Merge Detection Evidence
=============================================
Date: 2026-04-02

SCENARIO: verify that the compare harness detects field precedence mismatch
when a deprecated merge strategy would suppress a target field.

COMMAND:
  python3 compare_paths.py \
    --main-jsonl outputs/run_pipeline_sample.jsonl \
    --alt-jsonl outputs/find_attorney_mismatched.jsonl \
    --fields title,offices,department,practice_areas,industries \
    --report /tmp/cross_path_mismatch_test.json \
    --max-gap 0.10

OUTPUT:
  THRESHOLD BREACH: 1 field(s) exceed --max-gap 0.1: practice_areas

EXIT CODE: 1

This confirms that if the alternate path emitted an older merge result that
left practice_areas empty (simulating what the deprecated merge_attorney_fields
or the old ad-hoc _merge_external_data logic could produce), the compare harness
catches it immediately and exits non-zero, naming the offending field.

CONCLUSION: Both merge strategies are NOT simultaneously active.
The canonical path (FieldMerger) is the only active merge path in
_merge_external_data() after Task 9 changes.  merge_attorney_fields() is
DEPRECATED with no call sites in the production flow.
