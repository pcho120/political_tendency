TASK 9 — Alternate-Path Merge and Provenance Alignment
=======================================================
Date: 2026-04-02

CHANGES MADE
------------
1. find_attorney.py — merge_attorney_fields() (line 282):
   Marked DEPRECATED in docstring. FieldMerger (field_merger.py) is now
   documented as the ONE canonical merge path for the alternate architecture.
   The function is retained only to avoid breaking legacy call-sites outside
   the file; it will be removed in a future cleanup pass.

2. find_attorney.py — _merge_external_data() (line 4214):
   Rewrote to use self.field_merger.merge() instead of the previous ad-hoc
   field-by-field if-not-empty assignment.  The FieldMerger enforces the
   single authority rule (profile_core=100 > ... > external_directory=30),
   tracks provenance in merge_log, and writes source_origin_per_field into
   diagnostics so compare_paths.py / measure_baseline.py can see per-field
   sources.

3. find_attorney.py — FieldEnricher pass (line 4158):
   Removed the `in ('PARTIAL', 'FAILED')` status guard so FieldEnricher.enrich()
   is called for ALL profiles (not only partial/failed ones), consistently
   populating enrichment_log and field_sources in diagnostics.
   FieldEnricher is non-destructive: existing scalar values are kept; list
   fields are union-deduped.  The inner _apply_html_heuristics() is still
   guarded by profile._has_missing_fields(), so SUCCESS profiles with no
   missing fields skip the expensive heuristic pass.

CANONICAL MERGE AUTHORITY RULE (documented in _merge_external_data docstring)
--------------
  profile_core=100 > mixed=90 > attorney_list=80 > education/bar_admission=70
  > practice=60 > external_directory=30

DEAD CODE STATUS
----------------
- merge_attorney_fields(): DEPRECATED (no call sites in production flow)
- self.field_merger was instantiated but never called; it is now called
  from _merge_external_data(), resolving the dead-instance issue.

VERIFICATION
------------
Command:
  python3 compare_paths.py \
    --main-jsonl outputs/run_pipeline_sample.jsonl \
    --alt-jsonl outputs/find_attorney_sample.jsonl \
    --fields title,offices,department,practice_areas,industries \
    --report outputs/cross_path_diff_after_alignment.json \
    --max-gap 0.10

Exit code: 0

Report summary (all fields within 10pp threshold):
  title:         main=1.000  alt=1.000  gap=0.000  ✓
  offices:       main=1.000  alt=1.000  gap=0.000  ✓
  department:    main=0.063  alt=0.063  gap=0.000  ✓
  practice_areas: main=0.875  alt=0.813  gap=-0.063  ✓
  industries:    main=0.438  alt=0.500  gap=+0.063  ✓

Blocked firms excluded: Wilson Sonsini (BOT_PROTECTED)
fields_in_breach: []
all_within_threshold: true
