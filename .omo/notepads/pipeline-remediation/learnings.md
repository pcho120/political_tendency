- 2026-04-08: Added firm-level enrichment guard in `find_attorney.py` with `MAX_ENRICHMENT_TIME_PER_FIRM = 1800` beside `MAX_FIRM_TIME`, keeping `PLAYWRIGHT_PAGE_TIMEOUT` and discovery timeout unchanged.
- 2026-04-08: Shared-browser Playwright enrichment now uses a per-batch `threading.Event()` stop signal so worker threads stop between URLs and preserve whatever results were already collected.
- 2026-04-08: On enrichment timeout, the batch closes active browsers, returns partial results, and logs a warning with firm name plus partial-result count so downstream JSONL streaming in `run()` can still flush collected attorneys.
- 2026-04-08: SIGINT handling in `find_attorney.py` is safest when registered inside `run()`, sets a class shutdown flag, signals any active enrichment stop event, snapshots live Playwright browsers for best-effort close, then lets the main loop break so workbook/JSONL/summary/coverage saving still runs once at the end.
- 2026-04-08: Added `dedup_jsonl.py` to deduplicate JSONL by `profile_url` or `firm + full_name`, keeping the record with the most populated fields and skipping malformed lines with warnings.
- 2026-04-08: Added `--resume` to `find_attorney.py`; resume mode now loads firm names from existing `attorneys.jsonl`, opens JSONL in append mode, and skips already-processed firms with a `[RESUME]` log instead of reprocessing them.
- 2026-04-08: Resume mode also preloads `firm_level_summary.csv` and `coverage_metrics.json`, then merges by firm before final write so partial runs can restart without losing previously completed firms; default mode continues to open `attorneys.jsonl` in write mode and overwrite prior run output.
- 2026-04-08: Output saving in `run()` moved from `try` body into `finally` block for crash safety — Excel, summary CSV, coverage JSON, and JSONL flush/close now run even if an unhandled exception propagates from the firm loop; each save is independently wrapped in try/except so one failure doesn't block others.
- 2026-04-08: Added `AttorneyFinder._rebuild_excel_from_jsonl(jsonl_path, excel_path)` static method for post-crash recovery: reads all JSONL records, maps fields to the same 13-column Excel structure as `run()`, skips malformed lines with warnings, and logs errors without raising.

## 2026-04-08 Task 6: Department Inference
- Root cause: `find_attorney.py` has its own enrichment pipeline via `field_enricher.py` and `MultiModeExtractor`, completely bypassing `enrichment.py`'s `ProfileEnricher` which had the department inference call. So `infer_department_from_practices()` existed and worked, but was never reachable from the production pipeline.
- Fix: Added `from enrichment import infer_department_from_practices` to `find_attorney.py` and a 8-line inference loop after `field_enricher.enrich()` in `_enrich_profile_urls()` (lines 4590-4598).
- Simulation on existing JSONL (44,725 records): 26,167 of 33,298 dept-empty records (78.6%) would gain an inferred department with the existing 24 mappings. No new mappings needed.
- Per-firm projected gains: Baker McKenzie 99.7%, Gibson Dunn 98.5%, Latham 98.3%, Simpson Thacher 100%, Quinn Emanuel 100%. DLA Piper lower at 37.1% due to heavy nav-pollution in practice_areas. White & Case 0% (no practice_areas data).
- Key pattern: `find_attorney.py` and `enrichment.py` are two separate enrichment pipelines. Features added to one don't automatically appear in the other. Always check both paths when adding cross-cutting enrichment logic.
