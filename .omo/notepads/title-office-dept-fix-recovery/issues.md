# Issues — title-office-dept-fix-recovery

- Environment mismatch: `python3.12` is unavailable on this host, so verification had to fall back to `/usr/bin/python3` (`Python 3.13.7`). Fresh evidence reflects that interpreter explicitly.
